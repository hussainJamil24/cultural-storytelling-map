# Web framework
fastapi==0.136.3
uvicorn==0.49.0

# Form data parsing (needed for /register and /login form fields)
python-multipart==0.0.32

# Database
SQLAlchemy==2.0.50

# Data validation
pydantic==2.13.4

# Password hashing
passlib==1.7.4
# IMPORTANT: bcrypt must stay at 4.0.1 — bcrypt 5.x removes an attribute
# that passlib 1.7.4 depends on and breaks password hashing.
bcrypt==4.0.1

# JWT authentication (the [cryptography] extra pulls in the crypto backend)
python-jose[cryptography]==3.5.0

# Loads secrets (SECRET_KEY etc.) from a .env file
python-dotenv==1.2.2

# Testing
pytest==9.0.3
httpx==0.28.1
